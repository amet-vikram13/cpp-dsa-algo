#include <iostream>
#include <climits>
using namespace std;

int main()
{
    int q,n,max_pro,min_pri;
    int* arr;
    cin >> q;
    while(q--)
    {
        cin >> n;
        arr = new int[n];
        for(int i=0;i<n;i++)
            cin >> arr[i];
            
    } 
    return 0;
}